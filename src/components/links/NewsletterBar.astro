---
import { connect } from '../../data/links';
---

<section class="w-full">
  <div class="relative overflow-hidden rounded-xl bg-[#D6E6F2]/70 border border-[#B9D7EA] p-5 sm:p-6 backdrop-blur-md shadow-xl">
    
    <!-- Top Header of Connect section -->
    <div class="flex items-center gap-2.5 mb-2">
      <div class="w-8 h-8 rounded-lg bg-white border border-[#B9D7EA] text-[#769FCD] flex items-center justify-center shadow-sm">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
        </svg>
      </div>
      <h2 class="text-base font-bold text-slate-900 tracking-tight">
        {connect.title}
      </h2>
    </div>

    <!-- Description -->
    <p class="text-xs sm:text-sm text-slate-600 mb-4 leading-relaxed font-medium">
      {connect.description}
    </p>

    <!-- Connect Form with Email & Message Fields -->
    <form id="connect-form" class="flex flex-col space-y-3">
      <!-- Email Field -->
      <div>
        <label for="connect-email" class="block text-xs font-bold text-slate-700 mb-1">
          Email Address
        </label>
        <input
          id="connect-email"
          type="email"
          required
          placeholder={connect.emailPlaceholder}
          class="w-full bg-[#F7FBFC] border border-[#B9D7EA] focus:border-[#769FCD] focus:bg-white rounded-lg px-3.5 py-2.5 text-xs sm:text-sm text-slate-900 placeholder-slate-400 focus:outline-none focus:ring-1 focus:ring-[#769FCD] transition-all shadow-sm"
        />
      </div>

      <!-- Message Field -->
      <div>
        <label for="connect-message" class="block text-xs font-bold text-slate-700 mb-1">
          Your Message
        </label>
        <textarea
          id="connect-message"
          rows="2"
          required
          placeholder={connect.messagePlaceholder}
          class="w-full bg-[#F7FBFC] border border-[#B9D7EA] focus:border-[#769FCD] focus:bg-white rounded-lg px-3.5 py-2.5 text-xs sm:text-sm text-slate-900 placeholder-slate-400 focus:outline-none focus:ring-1 focus:ring-[#769FCD] transition-all shadow-sm resize-none"
        ></textarea>
      </div>

      <!-- Submit Button -->
      <button
        type="submit"
        class="inline-flex items-center justify-center gap-2 w-full py-2.5 px-4 rounded-lg font-bold text-xs sm:text-sm text-white bg-[#769FCD] hover:bg-[#648ebc] shadow-md shadow-[#769FCD]/25 transform hover:-translate-y-0.5 active:translate-y-0 transition-all duration-200 cursor-pointer"
      >
        <span>{connect.buttonText}</span>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
        </svg>
      </button>
    </form>
  </div>
</section>

<script>
  const form = document.getElementById('connect-form') as HTMLFormElement | null;
  if (form) {
    form.addEventListener('submit', (e) => {
      e.preventDefault();
      const emailInput = document.getElementById('connect-email') as HTMLInputElement | null;
      const email = emailInput?.value || 'your email';
      alert(`Thank you! Your message has been received from ${email}. I will get back to you shortly! 🚀`);
      form.reset();
    });
  }
</script>
